/**
 * Pesistence package. This package contains all persistence classes which can
 * be repositories, DAOs, finders or others. It can contain other structure
 * packages.
 */
package ${package}.persistence;